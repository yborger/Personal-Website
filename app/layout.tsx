import './globals.css';
import { Link } from 'react-router-dom';

export const metadata = {
  title: 'Website: Yael Borger',
  description: 'Website generated by create next app, designed and used by Yael Borger',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <div
          className="min-h-screen flex items-center justify-center bg-cover bg-center"
          style={{ backgroundImage: 'url(clouds.jpg)' }}
        >
          {/* Container of page content */}
          <div className="bg-white rounded-lg shadow-xl p-8 m-8 w-full h-screen">
            {/* Signature-ish Image at the Top */}
            <div className="flex justify-center mb-6">
              <img
                src="art sign.png"
                className="w-24 h-24 rounded-lg"
                alt="Signature"
              />
            </div>

            {/* Clickable Pages */}
            <div className="grid grid-cols-4 gap-4">
              <h1  className="flex items-center justify-center h-12 bg-white-200 hover:underline hover:bg-gray-200 transition-all duration-300">
                About
              </h1>
              <h1  className="flex items-center justify-center h-12 bg-white-200 hover:underline hover:bg-gray-200 transition-all duration-300">
                Contact
              </h1>
              <h1 className="flex items-center justify-center h-12 bg-white-200 hover:underline hover:bg-gray-200 transition-all duration-300">
                Coursework
              </h1>
              <h1 className="flex items-center justify-center h-12 bg-white-200 hover:underline hover:bg-gray-200 transition-all duration-300">
                Portfolio
              </h1>
            </div>

            {/* Render children content here */}
            <div className="mt-6">
              {children}
            </div>
          </div>
        </div>
      </body>
    </html>
  );
}